<form method="post" action="{{ path('admin_kanto_delete', {'id': kanto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kanto.id) }}">
    <button class="btn">Delete</button>
</form>
