{% extends 'base-admin.html.twig' %}

{% block title %}Kanto{% endblock %}

{% block body %}
    <h1>Kanto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kanto.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ kanto.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ kanto.sousTitre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ kanto.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_kanto_index') }}">back to list</a>

    <a href="{{ path('admin_kanto_edit', {'id': kanto.id}) }}">edit</a>

    {{ include('admin_kanto/_delete_form.html.twig') }}
{% endblock %}
