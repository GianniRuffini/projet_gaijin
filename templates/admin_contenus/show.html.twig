{% extends 'base-admin.html.twig' %}

{% block title %}Contenus{% endblock %}

{% block body %}
    <h1>Contenus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contenu.id }}</td>
            </tr>
            <tr>
                <th>TitreHebergement</th>
                <td>{{ contenu.titreHebergement }}</td>
            </tr>
            <tr>
                <th>SousTitreHebergement</th>
                <td>{{ contenu.sousTitreHebergement }}</td>
            </tr>
            <tr>
                <th>DescriptionHebergement</th>
                <td>{{ contenu.descriptionHebergement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_contenus_index') }}">back to list</a>

    <a href="{{ path('admin_contenus_edit', {'id': contenu.id}) }}">edit</a>

    {{ include('admin_contenus/_delete_form.html.twig') }}
{% endblock %}
