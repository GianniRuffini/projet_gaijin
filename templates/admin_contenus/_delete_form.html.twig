<form method="post" action="{{ path('admin_contenus_delete', {'id': contenu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu.id) }}">
    <button class="btn">Delete</button>
</form>
