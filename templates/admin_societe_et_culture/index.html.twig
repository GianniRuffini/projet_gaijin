{% extends 'base-admin.html.twig' %}

{% block title %}SocieteEtCulture index{% endblock %}

{% block body %}
    <h1>SocieteEtCulture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>SousTitre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for societe_et_culture in societe_et_cultures %}
            <tr>
                <td>{{ societe_et_culture.id }}</td>
                <td>{{ societe_et_culture.titre }}</td>
                <td>{{ societe_et_culture.sousTitre }}</td>
                <td>{{ societe_et_culture.description }}</td>
                <td>
                    <a href="{{ path('admin_societe_et_culture_show', {'id': societe_et_culture.id}) }}">show</a>
                    <a href="{{ path('admin_societe_et_culture_edit', {'id': societe_et_culture.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_societe_et_culture_new') }}">Create new</a>
{% endblock %}
