{% extends 'base-admin.html.twig' %}

{% block title %}DecalageHoraireAuJapon index{% endblock %}

{% block body %}
    <h1>DecalageHoraireAuJapon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>SousTitre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for decalage_horaire_au_japon in decalage_horaire_au_japons %}
            <tr>
                <td>{{ decalage_horaire_au_japon.id }}</td>
                <td>{{ decalage_horaire_au_japon.titre }}</td>
                <td>{{ decalage_horaire_au_japon.sousTitre }}</td>
                <td>{{ decalage_horaire_au_japon.description }}</td>
                <td>
                    <a href="{{ path('admin_decalage_horaire_au_japon_show', {'id': decalage_horaire_au_japon.id}) }}">show</a>
                    <a href="{{ path('admin_decalage_horaire_au_japon_edit', {'id': decalage_horaire_au_japon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_decalage_horaire_au_japon_new') }}">Create new</a>
{% endblock %}
