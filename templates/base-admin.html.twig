<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                Gaijin
            {% endblock %}
        </title>
        <link rel="stylesheet" href="{{ asset('css/font-awesome.min.css') }}">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.5.2/animate.min.css">
        <link rel="stylesheet" href="{{ asset('css/admin.min.css') }}">
        {% block stylesheets %}{% endblock %}

    </head>
</html>


    <body>

        {# HEADER #}
        <header class="text-dark py-3 px-3 d-flex justify-content-between" style="background:#edd34d">
            <div class="text-light">
                GAIJIN : Admin
            </div>
            <div class="d-flex pull-right mr-2">
                <span>
                    Bienvenue 
                    {{ app.user.email }}
                </span>
                <a class="ms-3 text-light btn btn-danger pull-right" href="{{ path('app_logout') }}">
                    Déconnexion
                </a>
            </div>
        </header>

        {#  FIN HEADER #}

        <div class="container-fluid">
            <div class="row">
                <div class="col-8 col-md-2 bg-admin-menu">
                    <ul class="list-unstyled">
                        <li>
                            <a href="{{ path('admin_dashboard') }}">
                                Dashboard
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('admin_user_index', { role: 'admin'}) }}">
                                Administrateurs
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('admin_user_index', { role: 'user'}) }}">
                                Utilisateurs
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('admin_home_index') }}">
                                Acceuil Gestion
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('admin_photo_index') }}">
                                Gestion Photos
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('admin_video_index') }}">
                                Gestion Videos
                            </a>
                        </li>
                    </ul>
                </div>
                <div class="col-12 col-md-10">
                    {% block body %}{% endblock %}
                </div>
            </div>
        </div>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-U1DAWAznBHeqEIlVSCgzq+c9gqGAJn5c/t99JyeKa9xxaYpSvHU5awsuZVVFIhvj" crossorigin="anonymous"></script>

        {% block javascripts %}{% endblock %}

    </body>

    {% block footer %}{% endblock %}

</html>

