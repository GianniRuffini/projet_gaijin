<form method="post" action="{{ path('admin_kansai_delete', {'id': kansai.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kansai.id) }}">
    <button class="btn">Delete</button>
</form>
