{% extends 'base.html.twig' %}

{% block title %}Kansai{% endblock %}

{% block body %}
    <h1>Kansai</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kansai.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ kansai.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ kansai.sousTitre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ kansai.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_kansai_index') }}">back to list</a>

    <a href="{{ path('admin_kansai_edit', {'id': kansai.id}) }}">edit</a>

    {{ include('admin_kansai/_delete_form.html.twig') }}
{% endblock %}
