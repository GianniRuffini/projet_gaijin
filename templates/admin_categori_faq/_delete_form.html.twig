<form method="post" action="{{ path('admin_category_faq_delete', {'id': category_faq.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_faq.id) }}">
    <button class="btn">Delete</button>
</form>
