<form method="post" action="{{ path('admin_category_home_delete', {'id': category_home.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_home.id) }}">
    <button class="btn">Delete</button>
</form>
