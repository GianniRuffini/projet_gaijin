{% extends 'base-admin.html.twig' %}

{% block title %}GuideDeVoyage{% endblock %}

{% block body %}
    <h1>GuideDeVoyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guide_de_voyage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ guide_de_voyage.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ guide_de_voyage.sousTitre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ guide_de_voyage.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_guide_de_voyage_index') }}">back to list</a>

    <a href="{{ path('admin_guide_de_voyage_edit', {'id': guide_de_voyage.id}) }}">edit</a>

    {{ include('admin_guide_de_voyage/_delete_form.html.twig') }}
{% endblock %}
