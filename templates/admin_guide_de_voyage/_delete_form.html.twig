<form method="post" action="{{ path('admin_guide_de_voyage_delete', {'id': guide_de_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide_de_voyage.id) }}">
    <button class="btn">Delete</button>
</form>
