{% extends 'base-admin.html.twig' %}

{% block title %}VisiteAuJapon index{% endblock %}

{% block body %}
    <h1>VisiteAuJapon index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>SousTitre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for visite_au_japon in visite_au_japons %}
            <tr>
                <td>{{ visite_au_japon.id }}</td>
                <td>{{ visite_au_japon.titre }}</td>
                <td>{{ visite_au_japon.sousTitre }}</td>
                <td>{{ visite_au_japon.description }}</td>
                <td>
                    <a href="{{ path('admin_visite_au_japon_show', {'id': visite_au_japon.id}) }}">show</a>
                    <a href="{{ path('admin_visite_au_japon_edit', {'id': visite_au_japon.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_visite_au_japon_new') }}">Create new</a>
{% endblock %}
