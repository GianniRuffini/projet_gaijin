{% extends 'base-admin.html.twig' %}


{% block title %}Transports{% endblock %}

{% block body %}
    <h1>Transports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transport.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ transport.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ transport.sousTitre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transport.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_transports_index') }}">back to list</a>

    <a href="{{ path('admin_transports_edit', {'id': transport.id}) }}">edit</a>

    {{ include('admin_transports/_delete_form.html.twig') }}
{% endblock %}
