<form method="post" action="{{ path('admin_budget_et_argent_au_japon_delete', {'id': budget_et_argent_au_japon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget_et_argent_au_japon.id) }}">
    <button class="btn">Delete</button>
</form>
