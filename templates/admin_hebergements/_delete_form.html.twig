<form method="post" action="{{ path('admin_hebergements_delete', {'id': hebergement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.id) }}">
    <button class="btn">Delete</button>
</form>
