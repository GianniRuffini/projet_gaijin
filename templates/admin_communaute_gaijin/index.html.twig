{% extends 'base-admin.html.twig' %}

{% block title %}CommunauteGaijin index{% endblock %}

{% block body %}
    <h1>CommunauteGaijin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for communaute_gaijin in communaute_gaijins %}
            <tr>
                <td>{{ communaute_gaijin.id }}</td>
                <td>{{ communaute_gaijin.titre }}</td>
                <td>
                    <a href="{{ path('admin_communaute_gaijin_show', {'id': communaute_gaijin.id}) }}">show</a>
                    <a href="{{ path('admin_communaute_gaijin_edit', {'id': communaute_gaijin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_communaute_gaijin_new') }}">Create new</a>
{% endblock %}
