<form method="post" action="{{ path('admin_communaute_gaijin_delete', {'id': communaute_gaijin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ communaute_gaijin.id) }}">
    <button class="btn">Delete</button>
</form>
