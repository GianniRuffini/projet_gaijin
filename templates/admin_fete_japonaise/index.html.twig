{% extends 'base-admin.html.twig' %}

{% block title %}FeteJaponaise index{% endblock %}

{% block body %}
    <h1>FeteJaponaise index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>SousTitre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fete_japonaise in fete_japonaises %}
            <tr>
                <td>{{ fete_japonaise.id }}</td>
                <td>{{ fete_japonaise.titre }}</td>
                <td>{{ fete_japonaise.sousTitre }}</td>
                <td>{{ fete_japonaise.description }}</td>
                <td>
                    <a href="{{ path('admin_fete_japonaise_show', {'id': fete_japonaise.id}) }}">show</a>
                    <a href="{{ path('admin_fete_japonaise_edit', {'id': fete_japonaise.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_fete_japonaise_new') }}">Create new</a>
{% endblock %}
