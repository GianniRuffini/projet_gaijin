{% extends 'base-admin.html.twig' %}

{% block title %}CuisineJaponaise{% endblock %}

{% block body %}
    <h1>CuisineJaponaise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuisine_japonaise.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ cuisine_japonaise.titre }}</td>
            </tr>
            <tr>
                <th>SousTitre</th>
                <td>{{ cuisine_japonaise.sousTitre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cuisine_japonaise.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cuisine_japonaise_index') }}">back to list</a>

    <a href="{{ path('admin_cuisine_japonaise_edit', {'id': cuisine_japonaise.id}) }}">edit</a>

    {{ include('admin_cuisine_japonaise/_delete_form.html.twig') }}
{% endblock %}
